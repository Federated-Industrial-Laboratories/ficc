{
  "name": "ficc-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node build.mjs",
    "test": "playwright test --config ../tests/browser/playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
